laserslicer.legradic.ch {

	# API
	reverse_proxy /api/* backend:8000

	# Everything not /api goes to the frontend container
	reverse_proxy / frontend:4173

	# AFTER the proxy, patch JS responses
	@js path *.js
	header @js Content-Type "application/javascript; charset=utf-8"
}